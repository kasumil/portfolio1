import React from 'react';
import { Icon } from '@iconify/react';

const Index = ({ icon, height, style }) => {
    return (
        <Icon
            icon={icon}
            height={height}
            style={style}
        />
    );
}

export default Index;
